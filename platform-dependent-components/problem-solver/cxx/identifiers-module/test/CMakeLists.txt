make_tests_from_folder(${CMAKE_CURRENT_LIST_DIR}/units
    NAME identifiers-module-tests
    DEPENDS sc-machine::sc-builder-lib identifiers-module
    INCLUDES ${IDENTIFIERS_MODULE_SRC}
)

if(${SC_CLANG_FORMAT_CODE})
    target_clangformat_setup(identifiers-module-tests)
endif()
